package agh.ics.oop;

import java.util.Arrays;

public class OptionsParser {
//    public static MoveDirection[] parse(String[] input){
//        int countValidInput = 0;
//        MoveDirection[] output = new MoveDirection[input.length];
//        for (int i = 0; i < input.length; i++){
//            switch (input[i]){
//                case "forward", "f" -> output[countValidInput] = MoveDirection.FORWARD;
//                case "backward", "b" -> output[countValidInput] = MoveDirection.BACKWARD;
//                case "right", "r" -> output[countValidInput] = MoveDirection.RIGHT;
//                case "left", "l" -> output[i] = MoveDirection.LEFT;
//                default -> throw new IllegalArgumentException(input[i] + " is not legal move specification.");
//            }
//            countValidInput += 1;
//        }
//
//        return Arrays.copyOfRange(output, 0, countValidInput);
//    }
}
